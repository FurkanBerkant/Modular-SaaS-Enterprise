/**
 * UI Controller katmanı — Thymeleaf view'larını ve web request routing'ini yönetir.
 *
 * Bu paket @ApplicationModule annotation'ı içermez.
 * Modulith composition root olarak tüm modülleri orchestrate eder.
 * Modül ihlali üretmez çünkü Modulith sadece @ApplicationModule
 * annotasyonu olan paketleri modül olarak tanır; bu paket kural dışıdır.
 */
package pro.turkninja.saas.controller;